% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_base.R
\name{kalman_filter_core}
\alias{kalman_filter_core}
\title{Core Kalman Filter for Linear State-Space Models}
\usage{
kalman_filter_core(
  Y_t,
  nu_t,
  H,
  N,
  mu_t,
  G,
  M,
  Sigma_0,
  rho_0,
  indic_pos = 0,
  Rfunction = Rf,
  Qfunction = Qf,
  reconciliationf = function(x, opt) {
     x
 }
)
}
\arguments{
\item{Y_t}{Matrix (\eqn{T \times n_y}) of observed series.}

\item{nu_t}{Matrix (\eqn{T \times n_\rho}) of state transition intercepts/deterministic shifts.}

\item{H}{Matrix (\eqn{n_\rho \times n_\rho}) state transition matrix.}

\item{N}{Matrix (\eqn{n_\rho \times \dots}) defining the state innovation covariance \eqn{Q = N N^\top}.}

\item{mu_t}{Matrix (\eqn{T \times n_y}) of exogenous observation intercepts.}

\item{G}{Matrix (\eqn{n_y \times n_\rho}) measurement mapping matrix.}

\item{M}{Matrix (\eqn{n_y \times \dots}) defining the measurement error covariance \eqn{R = M M^\top}.}

\item{Sigma_0}{Matrix (\eqn{n_\rho \times n_\rho}) initial prior state covariance.}

\item{rho_0}{Matrix (\eqn{n_\rho \times 1}) initial prior state vector.}

\item{indic_pos}{Vector or integer indicating indices of state variables subject to non-negativity constraints. Defaults to \code{0}.}

\item{Rfunction}{Function returning the measurement error covariance matrix \eqn{R_t}. Defaults to \code{Rf}.}

\item{Qfunction}{Function returning the state innovation covariance matrix \eqn{Q_t}. Defaults to \code{Qf}.}

\item{reconciliationf}{Optional reconciliation function applied after the measurement update step. Defaults to identity.}
}
\value{
A list containing:
\item{r}{Filtered posterior state trajectories (\eqn{\rho_{t|t}}).}
\item{Sigma_tt}{Flattened posterior state covariance matrices.}
\item{loglik}{Scalar cumulative log-likelihood value.}
\item{y_tp1_t}{Prior one-step-ahead measurement forecasts (\eqn{Y_{t|t-1}}).}
\item{S_tp1_t}{Flattened prior state covariance matrices (\eqn{\Sigma_{t|t-1}}).}
\item{r_tp1_t}{Prior one-step-ahead state forecasts (\eqn{\rho_{t|t-1}}).}
\item{loglik.vector}{Vector of step-by-step log-likelihood contributions.}
\item{Omega_tp1_t}{Flattened forecast error covariance matrices (\eqn{\Omega_{t|t-1}}).}
\item{M}{The measurement error factor matrix.}
\item{fitted.obs}{Fitted in-sample observation estimates (\eqn{\hat{Y}_{t|t}}).}
}
\description{
Evaluates the log-likelihood objective function and extracts filtered state trajectories
via the Kalman filter prediction error decomposition for linear Gaussian state-space models.
}
\details{
The state-space model is formulated by the following core system equations:

\bold{State (Transition) Equation:}
\deqn{\rho_t = \nu_t + H \rho_{t-1} + \eta_t, \quad \eta_t \sim \mathcal{N}(0, Q_t)}
where \eqn{Q_t = N N^\top}.

\bold{Observation (Measurement) Equation:}
\deqn{Y_t = \mu_t + G \rho_t + \varepsilon_t, \quad \varepsilon_t \sim \mathcal{N}(0, R_t)}
where \eqn{R_t = M M^\top}.

Missing observations in \eqn{Y_t} are handled dynamically via sub-matrix selection 
across the measurement update step.
}
\seealso{
\code{\link{Rf}}, \code{\link{Qf}}, \code{\link{kalman_filter_taylor}}
}
