% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecasting_functions.R
\name{forecast_okun_ssm}
\alias{forecast_okun_ssm}
\title{Out-of-Sample Forecasting for the Okun State-Space Model}
\usage{
forecast_okun_ssm(
  params_df,
  date_col = "quarter",
  exog_var_col = "log_gdp",
  forecast_h = 8,
  Y_data_df = Y_okun,
  target_variable = "unemp_rate",
  X_data = X_okun,
  gdp_gap_forecasts_input = gdp_gap_forecasts,
  use_true_gdp = FALSE
)
}
\arguments{
\item{params_df}{A data frame containing filtered parameters and state estimates across quarters
(\code{natural_rate}, \code{state_2_trend}, \code{beta1}, \code{beta2}, \code{beta3}, \code{phi}).}

\item{date_col}{Character. Column name identifying quarterly dates. Defaults to \code{"quarter"}.}

\item{exog_var_col}{Character. Column name for the exogenous log GDP series in \code{X_data}. Defaults to \code{"log_gdp"}.}

\item{forecast_h}{Integer. Out-of-sample forecast horizon in quarters. Defaults to \code{8}.}

\item{Y_data_df}{A data frame containing actual historical observations. Defaults to \code{Y_okun}.}

\item{target_variable}{Character. Column name of the observed series to forecast. Defaults to \code{"unemp_rate"}.}

\item{X_data}{A data frame containing historical exogenous variables. Defaults to \code{X_okun}.}

\item{gdp_gap_forecasts_input}{A data frame formatted as a lower-triangular forecast matrix containing real-time GDP projections. Defaults to \code{gdp_gap_forecasts}.}

\item{use_true_gdp}{Logical. Retained for interface consistency. Defaults to \code{FALSE}.}
}
\value{
A data frame in a triangle forecast matrix layout where the first column is \code{date_col}
  (target quarters) and subsequent columns contain projected values from each vintage origin.
}
\description{
Generates recursive out-of-sample unemployment rate forecasts across rolling vintage origins
using filtered Okun state-space model estimates and real-time GDP gap projections.
}
\details{
At each quarterly vintage origin \eqn{T}, the cyclical unemployment rate \eqn{\tilde{u}_{T+h|T}}
is projected recursively by combining autoregressive dynamics with distributed lags of the GDP gap:
\deqn{\tilde{u}_{T+h|T} = \phi \, \tilde{u}_{T+h-1|T} + \beta_1 y_{T+h} + \beta_2 y_{T+h-1} + \beta_3 y_{T+h-2}}
Level forecasts are reconstructed by adding the random-walk natural rate trend state:
\deqn{\hat{u}_{T+h|T} = \bar{u}_{T|T} + \tilde{u}_{T+h|T}}
}
