% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_input_preparation.R
\name{get_hp_gap_forecast_matrix}
\alias{get_hp_gap_forecast_matrix}
\title{Construct Real-Time HP Filter Gap Forecast Matrix}
\usage{
get_hp_gap_forecast_matrix(
  Y_df,
  fcst_df,
  val_col,
  date_col = "quarter",
  freq = 1600,
  is_already_logged = FALSE,
  burn_in = "1982 Q1"
)
}
\arguments{
\item{Y_df}{A data frame containing historical realized observations.}

\item{fcst_df}{A matrix or data frame containing out-of-sample level forecasts,
where columns represent vintage origins and rows (or first column) represent target dates.}

\item{val_col}{Character. Name of the column containing values in \code{Y_df}.}

\item{date_col}{Character. Column name identifying quarterly dates across data frames. Defaults to \code{"quarter"}.}

\item{freq}{Numeric. Smoothing parameter (\eqn{\lambda}) for the HP filter. Defaults to \code{1600}.}

\item{is_already_logged}{Logical. If \code{FALSE} (default), computes natural logarithms before HP filtering.}

\item{burn_in}{Character or \code{yearqtr}. Historical sample start date to mitigate starting-point filter bias.
Defaults to \code{"1982 Q1"}.}
}
\value{
A named list containing:
\item{forecast_matrix}{A data frame formatted as a lower-triangular matrix with target dates as rows and vintage origins as columns, where values represent real-time HP gap forecasts (in percentage terms, \eqn{\times 100}).}
\item{full_sample_gap}{A data frame containing the full-sample ex-post HP gap benchmark series.}
}
\description{
Generates a lower-triangular real-time HP gap forecast matrix across expanding
vintage origins. For each vintage quarter \eqn{T}, historical data up to \eqn{T}
is combined with out-of-sample level forecasts (\eqn{T+1, \dots, T+h}) and filtered
via \code{mFilter::hpfilter} to extract pseudo-real-time cyclical gaps and mitigate
filter endpoint bias.
}
