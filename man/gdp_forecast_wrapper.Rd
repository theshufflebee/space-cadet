% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_input_preparation.R
\name{gdp_forecast_wrapper}
\alias{gdp_forecast_wrapper}
\title{Generate Real-Time Rolling Matrix of GDP Forecasts}
\usage{
gdp_forecast_wrapper(
  fcast_start,
  fcast_end = NULL,
  data,
  date_col,
  gdp_col,
  horizon = 8,
  hp_lambda = 1600
)
}
\arguments{
\item{fcast_start}{Character, Date, or \code{yearqtr}. Initial forecast origin date.}

\item{fcast_end}{Optional character, Date, or \code{yearqtr}. Terminal forecast origin date.
If \code{NULL}, defaults to the maximum date available in \code{data}.}

\item{data}{A data frame containing the historical time series.}

\item{date_col}{Character. Column name identifying quarterly dates in \code{data}.}

\item{gdp_col}{Character. Column name containing GDP or log GDP values.}

\item{horizon}{Integer. Forecast horizon in quarters. Defaults to \code{8}.}

\item{hp_lambda}{Numeric. Smoothing parameter (\eqn{\lambda}) passed to downstream HP filter routines.
Defaults to \code{1600}.}
}
\value{
A data frame formatted as a lower-triangular forecast matrix where the first column
  is \code{date} (target quarters) and subsequent columns contain projected values from each vintage origin.
}
\description{
Simulates recursive real-time GDP forecasting across a sequence of historical
quarterly origins (vintages). At each origin \eqn{T}, historical data is restricted
to \eqn{t \le T}, an ARIMA model is fitted, and multi-step level projections
are mapped into a forecast triangle matrix.
}
\seealso{
\code{\link{forecast_gdp}}
}
